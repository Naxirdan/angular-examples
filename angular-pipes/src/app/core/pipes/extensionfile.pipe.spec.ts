import { ExtensionfilePipe } from './extensionfile.pipe';

describe('ExtensionfilePipe', () => {
  it('create an instance', () => {
    const pipe = new ExtensionfilePipe();
    expect(pipe).toBeTruthy();
  });
});
